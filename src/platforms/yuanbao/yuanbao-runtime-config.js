/**
 * Yuanbao's visible composer requires the browser page to follow the real
 * headed-window dimensions. This is platform configuration, not selector or
 * adapter logic.
 */
export const yuanbaoRuntimeConfig = Object.freeze({ viewport: null });
